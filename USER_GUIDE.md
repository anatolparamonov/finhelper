# 📖 Руководство пользователя FinHelper

## 🚀 Быстрый старт

### 1️⃣ Обычный ввод данных
```
/start → СТАРТ → Расходы → 1000 → выбор категории → описание → подтверждение
```

### 2️⃣ Быстрый ввод данных
```
/test → - 1000 продукты магазин → подтверждение
```

## 📝 Форматы ввода

### ✅ Обычный режим (после выбора Расходы/Доходы)
**Вводите ТОЛЬКО сумму:**
- `1000` ✅
- `10 000` ✅ 
- `1000000` ✅

**НЕ вводите:**
- `- 1000 продукты` ❌ (это для команды /test)
- `1000 рублей` ❌
- `тысяча` ❌

### ✅ Быстрый режим (команда /test)
**Формат: знак + сумма + категория + описание**
- `+ 1000 продукты магазин` ✅ (доход)
- `- 5000 транспорт` ✅ (расход)
- `+50000 зарплата` ✅ (без пробела тоже можно)

## 🔄 Команды

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/test` | Быстрый ввод данных |
| `/plan` | Ввод запланированных расходов/доходов |
| `/report` | Получить ссылку на таблицу |
| `/reminders` | Настройка напоминаний (только админы) |
| `/help` | Справка |

## 📅 Напоминания

Бот автоматически отправляет напоминания:
- 🌅 **Утром** - "Запишем расходы?"
- 🌙 **Вечером** - "Подведем итоги дня?"

## ❌ Частые ошибки

### Проблема: "Неверный формат числа"
**Причина:** Вы ввели `- 500 продукты` в обычном режиме

**Решение:**
1. Для обычного режима: введите только `500`
2. Для быстрого ввода: используйте `/test`, затем `- 500 продукты`

### Проблема: "Категории не отображаются"
**Причина:** Возможно, проблема с Google Sheets

**Решение:**
1. Проверьте, что лист "Категории" существует
2. Убедитесь, что категории заполнены в столбцах A (доходы) и B (расходы)
3. Перезапустите бота командой `/restart` (только админы)

### Проблема: Бот "завис"
**Причина:** Неожиданная ошибка или неправильный ввод

**Решение:**
1. Используйте `/start` для возврата в начало
2. Или `/cancel` для отмены текущей операции

## 💡 Советы

### 🎯 Эффективное использование
- Используйте `/test` для быстрого ввода повседневных трат
- Используйте обычный режим для детального ввода с выбором категорий
- Используйте `/plan` для планирования бюджета

### 📊 Организация данных
- Заполните категории в Google Sheets заранее
- Используйте понятные названия категорий
- Регулярно проверяйте отчеты через `/report`

### ⏰ Напоминания
- Настройте удобное время через `/reminders`
- Используйте кнопку "СТАРТ" в напоминаниях для быстрого доступа

## 🆘 Получение помощи

1. **Справка:** `/help`
2. **Проблемы с ботом:** обратитесь к администратору
3. **Проблемы с таблицей:** проверьте права доступа в Google Sheets

## 📱 Примеры использования

### Утренний кофе
```
/test
- 150 кафе утренний кофе
```

### Покупка продуктов
```
/start → СТАРТ → Расходы → 2500 → Продукты → еженедельная закупка → Подтверждаю
```

### Получение зарплаты
```
/test
+ 50000 зарплата
```

### Планирование отпуска
```
/plan → Расходы → 100000 → Отдых → отпуск в Турции → Подтверждаю
```
